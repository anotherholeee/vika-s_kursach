# Отчет о проверке кода на соответствие требованиям

## ✅ Выполненные требования

### 1. Пользовательский интерфейс с меню
**Статус:** ✅ **ВЫПОЛНЕНО**
- Реализовано меню для гостевого режима (`displayGuestMenu`)
- Реализовано меню для административного режима (`displayAdminMenu`)
- Реализовано главное меню входа (`displayLoginMenu`)
- Все меню имеют удобную структуру и необходимые пункты

### 2. Работа с файлами (текстовые и бинарные)
**Статус:** ✅ **ВЫПОЛНЕНО**
- **Текстовые файлы:** `stops.txt`, `vehicles.txt`, `drivers.txt`, `routes.txt`, `trips.txt`
- **Бинарный файл:** `admins.bin` (для учетных данных администраторов)
- Реализован класс `DataManager` для сохранения/загрузки данных
- Файлы связаны через структуру данных системы
- Бинарный файл используется для хранения пар логин/пароль администраторов

### 3. Информация о маршрутах: откуда - куда, по каким дням
**Статус:** ✅ **ВЫПОЛНЕНО**
- Класс `Route` содержит:
  - Поле `startStop` и `endStop` (откуда - куда)
  - Поле `std::set<int> weekDays` для хранения дней недели (1-понедельник, 7-воскресенье)
  - Метод `operatesOnDay(int day)` для проверки работы в конкретный день
- Класс `Trip` содержит:
  - Поле `int weekDay` для указания дня недели выполнения рейса
  - Метод `getWeekDay()` для получения дня недели
- Информация сохраняется в файлах и загружается из них

### 4. Функции выдачи информации и добавления
**Статус:** ✅ **ВЫПОЛНЕНО**
- Функции выдачи информации:
  - `findRoutes()` - поиск маршрутов между остановками
  - `getStopTimetable()` - расписание для остановки
  - `viewTransportSchedule()` - просмотр расписания транспорта
  - `searchRoutesWithTransfers()` - поиск маршрутов с пересадками
  - `calculateArrivalTimes()` - расчет времени прибытия
- Функции добавления:
  - `addRoute()`, `addTrip()`, `addVehicle()`, `addStop()`, `addDriver()`
- Функции удаления:
  - `removeRoute()`, `removeTrip()`, `removeVehicle()`, `removeStop()`, `removeDriver()`

### 5. Операция отмены (Undo)
**Статус:** ✅ **ВЫПОЛНЕНО**
- Реализован паттерн Command с классами команд:
  - `AddRouteCommand`, `RemoveRouteCommand`
  - `AddTripCommand`, `RemoveTripCommand`
  - `AddVehicleCommand`, `RemoveVehicleCommand`
  - `AddStopCommand`, `RemoveStopCommand`
  - `AddDriverCommand`, `RemoveDriverCommand`
- Реализован класс `CommandHistory` для управления историей команд
- Все операции добавления/удаления поддерживают undo
- Метод `undo()` доступен в административном меню (пункт 14)
- Реализована проверка возможности отмены через `canUndo()`

### 6. Иерархия классов с наследованием (3+ уровня)
**Статус:** ✅ **ВЫПОЛНЕНО**
- **Иерархия транспортных средств (3 уровня):**
  - Уровень 1: `Vehicle` (базовый класс)
  - Уровень 2: `ElectricTransport`, `FuelTransport` (промежуточные классы)
  - Уровень 3: `Tram`, `Trolleybus` (наследуются от `ElectricTransport`), `Bus` (наследуется от `FuelTransport`)
- **Иерархия исключений (2 уровня):**
  - Уровень 1: `TransportException` (базовый класс, наследуется от `std::exception`)
  - Уровень 2: `InputException`, `FileException`, `ContainerException`
- Все уровни иерархии активно используются в коде

### 7. Контейнеры, итераторы и алгоритмы (свои и STL)
**Статус:** ✅ **ВЫПОЛНЕНО**
- **Свой контейнер:** `DynamicArray<T>` с собственным итератором
- **Свой итератор:** `DynamicArray<T>::Iterator` (random_access_iterator)
  - Реализованы все необходимые операции: `++`, `--`, `+`, `-`, `[]`, сравнения
  - Совместим со STL алгоритмами
- **STL контейнеры:** `vector`, `map`, `unordered_map`, `set`, `queue`
- **STL алгоритмы:** 
  - `std::sort` - сортировка расписаний и маршрутов
  - `std::find` - поиск элементов в контейнерах
  - `std::find_if` - поиск с условием
  - `std::min_element` - поиск минимального элемента
  - `std::remove_if` - удаление элементов по условию
  - `std::distance` - вычисление расстояния между итераторами
- Используются как свои, так и STL контейнеры и алгоритмы

### 8. Обработка исключительных ситуаций
**Статус:** ✅ **ВЫПОЛНЕНО**
- Реализована иерархия исключений:
  - `TransportException` (базовый класс, наследуется от `std::exception`)
  - `InputException` (ошибки ввода данных)
  - `FileException` (ошибки работы с файлами)
  - `ContainerException` (ошибки контейнеров и данных)
- Исключения обрабатываются в try-catch блоках во всех критических местах
- Исключения используются для валидации входных данных
- Обработка исключений реализована в пользовательском интерфейсе (гостевой и административный режимы)

### 9. Основные алгоритмы (не add/remove/view)
**Статус:** ✅ **ВЫПОЛНЕНО**
- **`JourneyPlanner::findJourneysWithTransfers()`** - поиск маршрутов с пересадками (алгоритм BFS - поиск в ширину)
  - Использует очередь для обхода графа остановок
  - Учитывает время отправления и пересадки
  - Возвращает все возможные варианты маршрутов
- **`JourneyPlanner::findFastestJourney()`** - поиск самого быстрого маршрута
  - Использует `findJourneysWithTransfers()` и выбирает самый быстрый
- **`JourneyPlanner::findJourneyWithLeastTransfers()`** - поиск маршрута с минимальными пересадками
  - Использует `std::min_element` для поиска маршрута с минимальным количеством пересадок
- **`calculateArrivalTimes()`** - расчет времени прибытия на остановки
  - Вычисляет время прибытия на каждую остановку маршрута
  - Учитывает среднюю скорость и время остановки
- **`findRoutes()`** - поиск маршрутов между остановками
  - Проверяет наличие остановок в маршруте
  - Проверяет порядок остановок (откуда - куда)

---

## 📊 Итоговая оценка

| Требование | Статус |
|-----------|--------|
| Пользовательский интерфейс с меню | ✅ Выполнено |
| Работа с файлами (текстовые и бинарные) | ✅ Выполнено |
| Информация о маршрутах: откуда - куда, по каким дням | ✅ Выполнено |
| Функции выдачи информации и добавления | ✅ Выполнено |
| Операция отмены (Undo) | ✅ Выполнено |
| Иерархия классов (3+ уровня) | ✅ Выполнено |
| Контейнеры, итераторы, алгоритмы | ✅ Выполнено |
| Обработка исключений | ✅ Выполнено |
| Основные алгоритмы (не add/remove/view) | ✅ Выполнено |

**Общий результат:** 9 из 9 требований выполнено (100%) ✅

---

## 📝 Детали реализации

### Иерархия классов Vehicle (3 уровня)
```cpp
Vehicle (базовый класс)
├── ElectricTransport (уровень 2)
│   ├── Tram (уровень 3)
│   └── Trolleybus (уровень 3)
└── FuelTransport (уровень 2)
    └── Bus (уровень 3)
```

### Дни недели в Route
- Хранятся в `std::set<int> weekDays` (1-понедельник, 7-воскресенье)
- Метод `operatesOnDay(int day)` проверяет работу маршрута в конкретный день
- Сохраняются в файл `routes.txt` при сериализации

### Дни недели в Trip
- Хранится в `int weekDay` (1-понедельник, 7-воскресенье)
- Метод `getWeekDay()` возвращает день недели рейса
- Сохраняется в файл `trips.txt` при сериализации

### Основные алгоритмы
1. **BFS для поиска маршрутов с пересадками** - использует очередь для обхода графа остановок
2. **Поиск самого быстрого маршрута** - сортировка по времени в пути
3. **Поиск маршрута с минимальными пересадками** - поиск минимального элемента по количеству пересадок
4. **Расчет времени прибытия** - вычисление времени на основе скорости и расстояния

### Контейнеры и итераторы
- **Свой контейнер:** `DynamicArray<Stop>` используется для хранения остановок
- **Свой итератор:** `DynamicArray<T>::Iterator` с полной поддержкой random_access_iterator
- **STL контейнеры:** используются для маршрутов, рейсов, транспорта, водителей

### Обработка исключений
- Все критические операции обернуты в try-catch блоки
- Исключения используются для валидации данных
- Пользователь получает понятные сообщения об ошибках

---

## ✅ Заключение

Все требования выполнены полностью. Код соответствует всем указанным критериям:
- Удобный пользовательский интерфейс
- Работа с текстовыми и бинарными файлами
- Информация о маршрутах с указанием дней недели
- Функции выдачи информации и добавления
- Операция отмены (Undo)
- Иерархия классов с 3+ уровнями наследования
- Собственные и STL контейнеры, итераторы и алгоритмы
- Обработка исключительных ситуаций
- Основные алгоритмы (не add/remove/view)
